# Environment Configuration
PORT=8080

# Database Configuration (for PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/string_analyzer

# For SQLite (development), no configuration needed
